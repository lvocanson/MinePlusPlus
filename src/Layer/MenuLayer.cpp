#include "MenuLayer.h"
#include "MinesweeperLayer.h"

EventConsumed MenuLayer::listenEvent(const sf::Event& event)
{
	return EventConsumed();
}

void MenuLayer::render(sf::RenderTarget& target) const
{
}
